export interface IProductSizeParams {
    shop_products_id: number, 
    param_one: number, 
    param_two: number | null, 
    param_three: number | null,
    width: number, 
    number: number, 
    weight: number,
    density?: number
}

export interface IProductSizeData {

}